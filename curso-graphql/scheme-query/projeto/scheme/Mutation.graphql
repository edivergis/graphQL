type Mutation {
    #vírgula opcional
    novoUsuario(dados: UsuarioInput): Usuario!

    excluirUsuario(filtro: UsuarioFiltro!): Usuario

    alterarUsuario (filtro:UsuarioFiltro, input:UsuarioInput): Usuario
}